# Core (Phase 1)
feedparser>=6.0
requests>=2.31
PyYAML>=6.0
streamlit>=1.30
pandas>=2.0
pydeck>=0.9   # already a transitive streamlit dep; pinned explicitly since
              # dashboard/app.py now imports it directly for the AIS/ADS-B map

# Phase 2 — daily brief (optional at runtime; only used if ANTHROPIC_API_KEY set)
anthropic>=0.40

# Phase 3 — AIS collector (optional; only used by scripts/collect_ais.py)
websockets>=12.0

# Local dev convenience: load .env automatically
python-dotenv>=1.0
